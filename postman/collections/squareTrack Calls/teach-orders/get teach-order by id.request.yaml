$kind: http-request
url: "{{api-url}}/api/teach-order/{{teachOrderId}}"
method: GET
order: 2000
description: |-
  GET /api/teach-order/:id — teach order with all entries (and FASR children).
  - 200 found.
  - 404 not found.
  - 406 invalid ID.
