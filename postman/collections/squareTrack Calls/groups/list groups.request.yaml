$kind: http-request
url: "{{api-url}}/api/group/list"
method: GET
order: 1000
description: |-
  GET /api/group/list — 200 with data: array (empty = []).
  Note: route prefix is `/api/group` (singular), not `/api/groups`.
