{
  "name": "api",
  "version": "0.1.8",
  "main": "index.js",
  "keywords": [],
  "author": "Kevin McDaniel",
  "license": "ISC",
  "description": "Square Dance Uber App.  Calls, sequences, classes, dancers and more",
  "prisma": {
    "schema": "./src/prisma",
    "seed": "ts-node ./src/prisma/seed.ts"
  },
  "scripts": {
    "dev": "npx prisma generate && npx nodemon src/server.ts",
    "dev2": "npx prisma migrate deploy && npx prisma generate && npx nodemon",
    "migrate": "npx prisma migrate dev",
    "start:local": "npx nodemon /app/src/server.ts",
    "dev1": "",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "express": "^5.0.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.1",
    "nodemon": "^3.1.7",
    "prisma": "^6.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
