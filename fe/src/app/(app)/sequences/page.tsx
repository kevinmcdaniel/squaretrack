import { StubPage } from '@/ui/elements/StubPage';

export default function Page() {
  return (
    <StubPage title="Sequences">
      Browse saved sequences with a class / dance mode toggle. Class mode is
      proficiency-aware and filters to taught calls; dance mode is tip-ready.
    </StubPage>
  );
}
