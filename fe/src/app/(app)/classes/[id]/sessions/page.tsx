import { StubPage } from '@/ui/elements/StubPage';

export default function Page() {
  return (
    <StubPage title="Sessions">
      Per-session history: sequences used, tip ratings, and proficiency updates.
    </StubPage>
  );
}
